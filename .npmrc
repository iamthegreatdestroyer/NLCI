; npm registry configuration
registry=https://registry.npmjs.org/

; Scope configuration for @nlci packages
@nlci:registry=https://registry.npmjs.org/

; Publish settings
access=public
legacy-peer-deps=true

; For GitHub Actions: use NPM_TOKEN environment variable
; For local: set token with: npm config set //registry.npmjs.org/:_authToken YOUR_TOKEN
