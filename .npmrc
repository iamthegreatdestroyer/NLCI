; npm registry configuration
registry=https://registry.npmjs.org/

; Scope configuration for @nlci packages
@nlci:registry=https://registry.npmjs.org/

; Publish settings
access=public
legacy-peer-deps=true

; Authentication token for publishing (set via environment variable or npm login)
; //registry.npmjs.org/:_authToken=${NPM_TOKEN}
